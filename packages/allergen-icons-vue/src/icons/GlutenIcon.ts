import iconData from '@kaspernowak/allergen-icons/icons/GlutenIcon'
import { createAllergenIcon } from '../createAllergenIcon'

const GlutenIcon = createAllergenIcon(iconData)

export default GlutenIcon
