import iconData from '@kaspernowak/allergen-icons/icons/LupinIcon'
import { createAllergenIcon } from '../createAllergenIcon'

const LupinIcon = createAllergenIcon(iconData)

export default LupinIcon
