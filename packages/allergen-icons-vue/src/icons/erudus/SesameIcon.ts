import iconData from '@kaspernowak/allergen-icons/icons/erudus/SesameIcon'
import { createAllergenIcon } from '../../createAllergenIcon'

const SesameIcon = createAllergenIcon(iconData)

export default SesameIcon
